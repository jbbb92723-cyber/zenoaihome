# 2026-08-13 角色知识体系与知识库治理

## 目标

把赞诺内容资产库的主线统一为：

```text
OPC 公开实践
→ 经验资产化
→ 角色知识体系
→ 内容、工具、服务与产品
→ 星火者行动、验证与协作
→ 职业自主权
```

## 导航治理

- 新增根级 `SOURCE_OF_TRUTH.md`，统一全库导航和冲突规则。
- 重写 `README.md`、`START.md`、`00_目录总规.md`、`Zeno OS 总首页.md`。
- 清除上述文件中指向已不存在目录和旧 `G:/` 路径的当前运行指令。
- 将 `内容工厂SOP.md` 从“钩子 + 强制变现”改为“角色 + 目标 + 问题 + 技能 + 知识 + 证据”。

## 星火者治理

- 新增 `星火者/README.md` 作为当前真源入口。
- 统一口径：当前是小规模付费实践共同体，长期建设为 OPC 分布式协作网络。
- 将角色清晰、经验资产、真实行动、项目复盘和可信协作写入核心交付。
- V1.0、V2.0、V2.1 旧总手册移入 `星火者/归档/`。
- 删除可由 Markdown 再生成的 HTML 派生文件和一次性合并/转换脚本。

## 内容单元治理

- 删除 10 个明显错误的“发布文案”自动抽取占位单元。
- 删除 2 份只引用上述错误单元的主题地图和 1 份错误装配稿。
- 原始发布文案保留，未删除来源。
- 重建关系和去重索引；治理后内容单元 50 个，主题地图 3 份。

## OPC 白皮书治理

- 保留 `内容资产/OPC白皮书/` 顶层正式命名的三卷 Markdown 与 PDF。
- 删除 `Kimi排版的/新建文件夹/` 和 `新建文件夹/` 中的完全重复 PDF。
- 两份字节级完全重复的 Markdown 副本已删除。
- Kimi 版的南宁 OPC Markdown 与当前长版不同，保留到 `内容资产/OPC白皮书/归档/旧排版/`。
- 删除无引用临时导出 `toP9C56.html` 与 `toP9C57.pdf`。

## 删除原则

本次只物理删除：

1. 经哈希确认的完全重复文件；
2. 无引用且可从真源再生成的派生物；
3. 内容明显错误、未完成却标为 `canonical: true` 的占位单元；
4. 与当前结构完全失配、且已无引用的系统体检文件。

原始素材、已发布内容、研究原件和有演变价值的旧版均保留。
